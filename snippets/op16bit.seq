    JSR $E544    LDA MESSAGE    LDX #0LOOP    STA $0405,X    INX    LDA MESSAGE,X    BNE LOOP    JSR ADD;   JSR SUB    JMP *ADD    CLC    LDA NUM1    ADC NUM2    STA RES    LDA NUM1+1    ADC NUM2+1    STA RES+1    RTSSUB    SEC    LDA NUM1    SBC NUM2    STA RES    LDA NUM1+1    SBC NUM2+1    STA RES+1    RTS    *= $3000RES    .WORD 0NUM1    .WORD 900NUM2    .WORD 250MESSAGE    .TEXT "CHECK MEMORY AT ADDRESS 3000"    .BYTE 0