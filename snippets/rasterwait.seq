;---------------------------------------;;    1 SECOND DELAY WAIT (PAL);;---------------------------------------     *= $1000DELAY = 50     SEIMLOOP     LDA #$FF     CMP $D012     BNE *-3     CMP $D012     BEQ *-3     DEC FRAMECNT     BNE MLOOP      ; COUNT REACHED?     INC $D020      ; CHG BORDER COLOR     LDA #DELAY     STA FRAMECNT   ; RESET FRM COUNTER     BNE MLOOPFRAMECNT     .BYTE DELAY